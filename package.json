{
  "name": "com.cn.tv-power",
  "version": "1.0.0",
  "description": "Stream Deck plugin — LG OLED TV power toggle",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --tsconfig=tsconfig.json --outfile=com.cn.tv-power.sdPlugin/bin/plugin.js",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
